#include <iostream>

int sumNumber(int n)
{

  int sum = 0;
  
  for (int i = 1; i <= n; i++)
  {
    i += sum;
  }

  return sum;
  
};

int main()
{
    int n;

    std::cout << "Enter a number: ";
    std::cin >> n;
    std::cout << std::endl;

    sumNumber(n);
    
    return 0;
};


